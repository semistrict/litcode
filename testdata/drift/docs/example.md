# Example

The hello function (lines are off by one — the source added a line above):

```go file=src/example.go lines=6-8
func hello() {
	fmt.Println("hello")
}
```

The goodbye function (also off by one):

```go file=src/example.go lines=10-12
func goodbye() {
	fmt.Println("goodbye")
}
```
